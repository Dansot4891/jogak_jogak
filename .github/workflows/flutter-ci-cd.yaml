name: Flutter CI/CD

on:
  push:
    branches:
      - main

env:
  # shorebird
  SHOREBIRD_TOKEN: ${{ secrets.SHOREBIRD_TOKEN }}
  # ios
  CERTIFICATE: ${{ secrets.IOS_CERTIFICATE }}
  CERTIFICATE_PASSWORD: ${{ secrets.IOS_CERTIFICATE_PASSWORD }}
  PROVISIONING_PROFILE: ${{ secrets.IOS_PROVISIONING_PROFILE }}
  KEYCHAIN_PASSWORD: ${{ secrets.IOS_KEYCHAIN_PASSWORD }}
  # android
  ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
  ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
  ANDROID_KEY_PASSWORD:   ${{ secrets.ANDROID_KEY_PASSWORD }}
  ANDROID_KEY_ALIAS:      ${{ secrets.ANDROID_KEY_ALIAS }}

jobs:
  # flutter-tests:
  #   name: Run Flutter Tests
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Checkout Repository
  #       uses: actions/checkout@v4

  #     - name: Set up Flutter
  #       uses: subosito/flutter-action@v2
  #       with:
  #         flutter-version: '3.29.3'

  #     - name: Install Dependencies
  #       run: flutter pub get

  #     - name: Run Code Generation
  #       run: flutter pub run build_runner build --delete-conflicting-outputs

  #     - name: Run Tests
  #       run: flutter test

  # detect-flutter-changes:
  #   name: Detect Flutter Code Changes
  #   runs-on: ubuntu-latest
  #   outputs:
  #     flutter_changed: ${{ steps.filter.outputs.flutter }}

  #   steps:
  #     - name: Checkout Repository
  #       uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0

  #     - name: List changed files (Debugging)
  #       run: |
  #         git diff --name-only ${{ github.event.before }} ${{ github.sha }}

  #     - name: Detect Changes
  #       uses: dorny/paths-filter@v3
  #       id: filter
  #       with:
  #         base: ${{ github.event.before }}
  #         ref: ${{ github.sha }}
  #         filters: |
  #           flutter:
  #             - 'lib/**'

  # # android shorebird release
  # release-android:
  #   runs-on: ubuntu-latest
  #   name: 🤖 Release Android

  #   steps:
  #     - name: 📚 Git Checkout
  #       uses: actions/checkout@v4
      
  #     - name: Set up Flutter
  #       uses: subosito/flutter-action@v2
  #       with:
  #         flutter-version: '3.29.3'

  #     - name: Install Dependencies
  #       run: flutter pub get

  #     - name: Run Code Generation
  #       run: flutter pub run build_runner build --delete-conflicting-outputs

  #     - name: ☕️ Setup Java
  #       uses: actions/setup-java@v4
  #       with:
  #         java-version: "17"
  #         distribution: "temurin"
  #         cache: gradle

  #     - name: 🐦 Setup Shorebird
  #       uses: shorebirdtech/setup-shorebird@v1
  #       with:
  #         cache: true

  #     # base64로 이루어진 keystore Decode
  #     - name: 🔐 Decode Keystore
  #       run: echo ${{ secrets.ANDROID_KEYSTORE_BASE64 }} | base64 --decode > ${{ github.workspace }}/keystore.jks

  #     - name: ✏️ Create key.properties
  #       run: |
  #         cat <<EOF > android/app/key.properties
  #         storePassword=${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
  #         keyPassword=${{ secrets.ANDROID_KEY_PASSWORD }}
  #         keyAlias=${{ secrets.ANDROID_KEY_ALIAS }}
  #         storeFile=${{ github.workspace }}/keystore.jks
  #         EOF

  #     - name: 🚀 Shorebird Release
  #       uses: shorebirdtech/shorebird-release@v0
  #       env:
  #         KEYSTORE_FILE: ${{ github.workspace }}/keystore.jks
  #         KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
  #         KEYSTORE_ALIAS:  ${{ secrets.ANDROID_KEY_ALIAS }}
  #       with:
  #         platform: android
  #         args: "--verbose --artifact=apk"

  #     - name: 📦 Upload APK
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: flutter_and_friends-${{ github.ref_name }}.apk
  #         path: build/app/outputs/apk/release/app-release.apk

  #     - name: 📦 Upload App Bundle
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: flutter_and_friends-${{ github.ref_name }}.aab
  #         path: build/app/outputs/bundle/release/app-release.aab

  # android shorebird patch
  patch-android:
    name: 🤖 Patch Android
    runs-on: ubuntu-latest

    steps:
      - name: 📚 Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.29.3'

      - name: Install Dependencies
        run: flutter pub get

      - name: Run Code Generation
        run: flutter pub run build_runner build --delete-conflicting-outputs

      - name: ☕️ Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: 🔧 Install Android SDK & Accept Licenses
        env:
          ANDROID_SDK_ROOT: ${{ runner.tool_cache }}/android-sdk
          ANDROID_HOME:     ${{ runner.tool_cache }}/android-sdk
        run: |
          sudo apt-get update
          sudo apt-get install -y unzip
          wget https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip -O cmdline-tools.zip
          mkdir -p $ANDROID_SDK_ROOT/cmdline-tools
          unzip cmdline-tools.zip -d $ANDROID_SDK_ROOT/cmdline-tools
          yes | $ANDROID_SDK_ROOT/cmdline-tools/cmdline-tools/bin/sdkmanager --sdk_root=$ANDROID_SDK_ROOT --licenses
          $ANDROID_SDK_ROOT/cmdline-tools/cmdline-tools/bin/sdkmanager --sdk_root=$ANDROID_SDK_ROOT \
            "platform-tools" "platforms;android-33" "build-tools;33.0.2"

      - name: 🐦 Setup Shorebird
        uses: shorebirdtech/setup-shorebird@v1
        with:
          cache: true

      - name: 🔐 Decode Keystore
        run: |
          echo "${{ env.ANDROID_KEYSTORE_BASE64 }}" \
            | base64 --decode > keystore.jks

      - name: ✏️ Create key.properties
        run: |
          cat <<EOF > android/app/key.properties
          storePassword=${{ env.ANDROID_KEYSTORE_PASSWORD }}
          keyPassword=${{ env.ANDROID_KEY_PASSWORD }}
          keyAlias=${{ env.ANDROID_KEY_ALIAS }}
          storeFile=${{ github.workspace }}/keystore.jks
          EOF

      # 1) 전체 파일 구조 확인
      - name: 🔍 Debug workspace structure
        run: |
          echo "🏷️ 현재 작업 디렉터리: $(pwd)"
          ls -alR .

      # 2) keystore.jks 확인 및 keytool로 내용 열람
      - name: 🔐 Defbug Keystore File
        run: |
          echo "· keystore.jks 파일 정보"
          ls -lah keystore.jks || true
          echo ""
          echo "· Keytool 리스트 (alias 확인)"
          keytool -list -v \
            -keystore keystore.jks \
            -storepass $ANDROID_KEYSTORE_PASSWORD || true

      # 3) key.properties 파일 위치·내용 확인
      - name: 📋 Debug key.properties
        run: |
          echo "· ANDROID_KEY_ALIAS 확인 (GitHub가 마스킹)"
          echo "ANDROID_KEY_ALIAS=$ANDROID_KEY_ALIAS"
          echo ""
          echo "· 파일 존재 여부"
          ls -lah android/key.properties android/app/key.properties || true
          echo ""
          echo "· 파일 내용"
          cat android/key.properties     && echo "" \
            || cat android/app/key.properties && echo "" \
            || echo "❗ key.properties 파일이 없습니다."

      # 4) gradlew 스크립트와 빌드 커맨드 실행
      - name: 🚨 Debug Gradle 직접 실행 (verbose)
        working-directory: android
        run: |
          echo "· gradlew 스크립트 존재 여부"
          ls -lah gradlew || true
          echo ""
          chmod +x gradlew || echo "⚠️ gradlew 파일을 찾을 수 없습니다."
          echo ""
          echo "· Gradle 빌드 (stacktrace + info + debug)"
          ./gradlew bundleRelease --stacktrace --info --debug

      - name: 🧩 Shorebird Patch
        id: shorebird-patch
        uses: shorebirdtech/shorebird-patch@v0
        env:
          SHOREBIRD_TOKEN:     ${{ env.SHOREBIRD_TOKEN }}
          KEYSTORE_FILE:       ${{ github.workspace }}/keystore.jks
          KEYSTORE_PASSWORD:   ${{ env.ANDROID_KEYSTORE_PASSWORD }}
          KEYSTORE_ALIAS:      ${{ env.ANDROID_KEY_ALIAS }}
        with:
          platform: android
          args: "--verbose --track=staging"


  # # ios shorebird release
  # release-ios:
  #   runs-on: macos-latest

  #   name: 🍎 Release iOS

  #   steps:
  #     - name: 📚 Git Checkout
  #       uses: actions/checkout@v4

  #     - name: Set up Flutter
  #       uses: subosito/flutter-action@v2
  #       with:
  #         flutter-version: '3.29.3'

  #     - name: Install Dependencies
  #       run: flutter pub get

  #     - name: Run Code Generation
  #       run: flutter pub run build_runner build --delete-conflicting-outputs

  #     - name: 🐦 Setup Shorebird
  #       uses: shorebirdtech/setup-shorebird@v1
  #       with:
  #         cache: true

  #     - name: ✍️ Setup XCode Signing
  #       run: |
  #         # create variables
  #         CERTIFICATE_PATH=$RUNNER_TEMP/build_certificate.p12
  #         KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
  #         PP_PATH=$RUNNER_TEMP/temp.mobileprovision

  #         # import certificate and provisioning profile from secrets
  #         echo -n "$CERTIFICATE" | base64 --decode -o $CERTIFICATE_PATH
  #         echo -n "$PROVISIONING_PROFILE" | base64 --decode -o $PP_PATH

  #         # create temporary keychain
  #         security create-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
  #         security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
  #         security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH

  #         # import certificate to keychain
  #         security import $CERTIFICATE_PATH -P "$CERTIFICATE_PASSWORD" -A -t cert -f pkcs12 -k $KEYCHAIN_PATH
  #         security list-keychain -d user -s $KEYCHAIN_PATH
  #         security find-identity -v

  #         # use uuid for the name of provisioning profile
  #         UUID=$(/usr/libexec/PlistBuddy -c "Print UUID" /dev/stdin <<< $(/usr/bin/security cms -D -i $PP_PATH))

  #         # apply provisioning profile
  #         mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
  #         cp $PP_PATH ~/Library/MobileDevice/Provisioning\ Profiles/$UUID.mobileprovision

  #     - name: 🚀 Shorebird Release
  #       uses: shorebirdtech/shorebird-release@v0
  #       with:
  #         platform: ios
  #         args: "--export-options-plist ios/GHAExportOptions.plist --verbose"

  #     - name: 🧹 Clean Up
  #       if: ${{ always() }}
  #       run: |
  #         PP_PATH=$RUNNER_TEMP/temp.mobileprovision
  #         security delete-keychain $RUNNER_TEMP/app-signing.keychain-db
  #         rm $RUNNER_TEMP/temp.mobileprovision

  #     - name: 📦 Upload Artifact
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: flutter_and_friends-${{ github.ref_name }}.ipa
  #         path: build/ios/ipa/flutter_and_friends.ipa

  # ios shorebird patch
  # patch-ios:
  #   name: 🍎 Patch iOS
  #   #needs: [flutter-tests, detect-flutter-changes]
  #   #if: needs.detect-flutter-changes.outputs.flutter_changed == 'true'
  #   runs-on: macos-latest

  #   steps:
  #     - name: 📚 Checkout repository
  #       uses: actions/checkout@v4

  #     - name: Set up Flutter
  #       uses: subosito/flutter-action@v2
  #       with:
  #         flutter-version: '3.29.3'

  #     - name: Install Dependencies
  #       run: flutter pub get

  #     - name: Run Code Generation
  #       run: flutter pub run build_runner build --delete-conflicting-outputs

  #     - name: 🐦 Setup Shorebird
  #       uses: shorebirdtech/setup-shorebird@v1
  #       with:
  #         cache: true

  #     - name: ✍️ Setup Xcode Signing
  #       run: |
  #         # Paths
  #         CERTIFICATE_PATH=$RUNNER_TEMP/build_certificate.p12
  #         KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
  #         PP_PATH=$RUNNER_TEMP/temp.mobileprovision

  #         # Decode & import
  #         echo -n "$CERTIFICATE" | base64 --decode -o $CERTIFICATE_PATH
  #         echo -n "$PROVISIONING_PROFILE" | base64 --decode -o $PP_PATH

  #         # Create & unlock an isolated keychain
  #         security create-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
  #         security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
  #         security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH

  #         # Import your certificate
  #         security import $CERTIFICATE_PATH -P "$CERTIFICATE_PASSWORD" -A -t cert -f pkcs12 -k $KEYCHAIN_PATH
  #         security list-keychain -d user -s $KEYCHAIN_PATH
  #         security find-identity -v

  #         # Verify identity
  #         UUID=$(/usr/libexec/PlistBuddy -c "Print UUID" /dev/stdin <<< $(/usr/bin/security cms -D -i $PP_PATH))

  #         # Install provisioning profile
  #         mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
  #         cp $PP_PATH ~/Library/MobileDevice/Provisioning\ Profiles/$UUID.mobileprovision

  #     - name: 🧩 Shorebird Patch iOS
  #       uses: shorebirdtech/shorebird-patch@v0
  #       id: shorebird-patch-ios
  #       with:
  #         platform: ios
  #         args: "--export-options-plist ios/GHAExportOptions.plist --track=staging"
